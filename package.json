{
  "name": "adrenaline-escapes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 -D -d dist/ src/",
    "build:dev": "babel -w --presets es2015 -D -d dist/ src/",
    "start": "node dist/server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "body-parser": "^1.16.1",
    "es6-promise": "^4.0.5",
    "express": "^4.14.1",
    "google-auth-library": "^0.10.0",
    "googleapis": "^16.1.0"
  }
}
